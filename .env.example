# Copy this file to .env and fill in a real key. .env is gitignored - never commit real credentials.
GEMINI_API_KEY=
# Optional. Only consulted as a last-resort fallback if GEMINI_API_KEY exhausts its
# quota on both the Pro and Flash tiers - see gemini_extractor.py's module docstring.
GEMINI_API_KEY_SECONDARY=
